fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
aiosqlite==0.21.0
apscheduler==3.10.4
requests==2.32.3
beautifulsoup4==4.12.3
pydantic>=2.10.4
python-dotenv==1.0.1
httpx==0.28.1
google-genai==0.3.0
deep-translator==1.11.4
redis==5.0.1
celery==5.3.6
prometheus-fastapi-instrumentator==7.0.0
watchdog==4.0.1
# ISO Life cycle 版本檢查（純 HTTP 主要路徑）：以 Chrome 的 TLS 指紋通過 iso.org 的
# Cloudflare Managed Challenge，不需要瀏覽器；詳見 crawlers/iso_http.py。
curl_cffi==0.15.0
# ISO Life cycle 版本檢查（虛擬瀏覽器後備路徑）：規避 Cloudflare CDP 偵測，需搭配本機 Chrome。
# 純 HTTP 路徑取得失敗時自動退回；設 ISO_HTTP_DISABLE=1 可強制一律走此路徑。
# 注意：nodriver 0.50.x 在 Python 3.14 下，其 cdp/network.py 含非 UTF-8 註解位元組會
#       導致 import 失敗；安裝後若 import 報 SyntaxError(Non-UTF-8)，將該檔內所有 >=128
#       的位元組替換為 '?' 即可（純註解，不影響功能）。詳見 scripts/iso_lifecycle_check.py。
nodriver==0.50.3
