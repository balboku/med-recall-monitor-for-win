# MedWatch 醫療器材監控系統 — 開發守則

FastAPI（backend）＋ React/Vite（frontend）＋ SQLite。核心功能是「法規標準追蹤」：
到各國官網比對公司持有的法規版本是否落後，並判定家族文件是否齊全。

---

## 1. 行為守則（優先於一切）

### 使用者說「你改壞了」時，第一步是讀自己的改動

**禁止**先歸因於快取、HMR、瀏覽器、作業系統設定或使用者環境。順序固定：

1. `git diff` 看自己這次改了什麼
2. 重讀使用者**原始那句需求**，確認自己有沒有超譯
3. 才考慮環境因素

**渲染結果符合自己的預期 ≠ 改對了。** 使用者說壞了而畫面符合你的預期，
結論是「我的預期錯了」，不是「使用者的環境錯了」。

> 實際事故：曾為 IEC 加了一組專屬灰階調色盤，把 5 種狀態全壓成灰白、語意配色全毀。
> 使用者指出後，連續用「HMR 沒更新 → 快取 → 無痕模式 → Windows 色彩濾鏡」往外歸因，
> 期間跑過 `git diff --stat` 看到自己改了 114 行仍未回頭檢視，浪費大量往返。

### 不要擴張需求範圍

- **不得**為單一類別新增專屬調色盤或視覺分支，除非明文要求
- 本專案充滿 `IEC_PRODUCT_FORMS`、`flavor == "iec"` 這類「特例對照表」，
  新增特例會顯得很符合慣例——**這是陷阱**，先確認是否真的需要
- 照字面執行會破壞既有功能時（例如壓掉語意配色），先講後果再問，不要直接做

---

## 2. 環境陷阱

| 陷阱 | 說明 |
| :--- | :--- |
| **改後端必須重啟** | 專案在 OneDrive 資料夾，`--reload` 檔案監看不可靠，`start_backend_local.bat` 刻意不帶 `--reload`。改 `backend/**.py` 後要 Ctrl+C 重跑。 |
| **真正的資料庫** | `backend/data/monitor.db`。根目錄的 `backend/medwatch.db`、`backend/monitor.db` 是 **0 張表的空殼**，別誤用。 |
| **測試不可動正式資料庫** | 複製到 scratchpad，再 `import config; config.DATABASE_PATH = Path(...)`（必須在 `import database` 之前）。 |
| **MD 匯入會清空官方網址** | `import_standards` 以前端送來的空字串覆蓋 `source_url`。匯入後第一次掃描必須用**虛擬瀏覽器搜尋模式**取回網址，否則例行模式會因無網址而全部略過。 |
| **MD 匯入會吃掉整份檔案的表格** | 解析器從第一個表頭之後把**所有** `\|` 分隔列都當標準資料。說明或待確認事項章節**不得出現 `\|`**，改用條列。 |

---

## 3. 架構鐵則

- **兩種掃描模式（`routine` 例行／`browser` 搜尋）必須走同一套判定管線。**
  新增來源時兩邊都要接。歷史上 ISO 與 IEC 各因此漏接一次，導致「缺件／修訂中」
  在日常掃描永遠不出現。
- **所有寫入路徑都要同時寫入 `judge_label` 與 `judge_categories`。**
  前端優先讀 `judge_categories`；只寫 label 會讓複數狀態被壓成單一狀態。
- **「查無結果」不是「有更新」**：`has_update` 寫 0（不計入統計），但仍要建立
  `standard_not_found` 警示。
- **不確定時輸出「⚪ 無法判定」，不得臆測**。不可用單一年份猜測持有版本。
- `category` 欄位的**字串必須完全相符**（含空白與斜線，如 `EN ISO / EN`、
  `ASTM / AAMI`），`standards.py` 的 `_standard_source()` 靠它分派來源，填錯會查錯官網。

### 判定邏輯的檔案分工

| 檔案 | 職責 |
| :--- | :--- |
| `backend/crawlers/standards_common.py` | 共用判定核心：`judge_update()`、文件類型、家族完整性、改版預告、結果合併 |
| `backend/crawlers/standards.py` | 依類別分派來源、兩種掃描模式、回寫資料庫、建立警示 |
| `backend/crawlers/{iso_browser,iec_api,en_harmonised,...}.py` | 各來源專屬：抓取與該來源的版本語意 |
| `frontend/src/pages/Standards.jsx` | 狀態徽章、分類統計、版本欄位顯示（957 行，顏色邏輯散在 4 處） |

---

## 4. 動判定邏輯前必讀

`需求/` 是本專案的規格書（17 份、約 2900 行）。**修改判定邏輯前必須先讀對應文件**，
不要憑程式碼推測意圖：

- `需求/法規標準追蹤/法規版本判定通用規則.md` — 所有類別共用（狀態分類、複數狀態、
  同類型比對、查無結果處理）。**個別文件與通用規則衝突時，以個別文件為準。**
- `需求/法規標準追蹤/{ISO,IEC,EN,EU,MDCG,FDA,台灣,ASTM}*.md` — 各類別專屬判定
- `需求/法規清單/外來文件清單轉法規清單規則.md` — xlsx → MD 清單的轉檔規則

### 各來源的 `+` 與 `/` 語意不同（最常誤判處）

| 來源 | `+` | `/` |
| :--- | :--- | :--- |
| ISO | 修正案本身（`2019+Amd 1:2022` 只持有 AMD） | 同左 |
| IEC | 本體已併入修正案的**合併版**（＝主標準等價物） | 掛在本體下的**修正案本身** |
| EN | 本體已含該修正案 | 附屬文件（`/AC` 勘誤、`/A11` 修正案） |

`norm_doc()` 會把 `+` 和 `/` 一起抹掉，**必須先依來源慣例分辨寫法再做正規化比對**。

---

## 5. 常用指令

```bash
# 一鍵安裝並啟動（首次）
.\restart_system.bat

# 分別啟動
.\start_backend_local.bat      # http://localhost:8000
.\start_frontend_local.bat     # http://localhost:5173

# 前端 lint
npm run lint --prefix frontend
```

後端無測試套件；驗證判定邏輯的方式是複製資料庫到 scratchpad 後跑
`crawlers.<module>.resolve_many()` 實測，並與 `需求/` 文件的案例對照表核對。
